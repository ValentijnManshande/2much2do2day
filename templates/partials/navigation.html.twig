
<header class="navbar navbar-expand-lg flex-column flex-md-row navbar-dark bg-dark">
    <a class="navbar-brand" href={{ path('home')}}>2Much2Do2day</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto">
            <li class="nav-item">
                <a class="nav-link" href="{{ path('list_overview')}}">Lijsten</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('user_overview')}}">Gebruikers</a>
            </li>
        </ul>

        {% if is_granted('IS_AUTHENTICATED_FULLY') %}
        <span class="navbar-text">
            Gegroet, {{ app.user.email}}
        </span>
        <ul class="navbar-nav">
            <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                Mijn account
                </a>
                <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                    <a class="dropdown-item" href="{{ path('my_profile') }}">Mijn profiel</a>
                    <a class="dropdown-item" href="{{ path('my_list') }}">Mijn lijsten</a>
                    <div class="dropdown-divider"></div>
                    <a class="dropdown-item" href="{{ path('app_logout') }}">Uitloggen</a>
                </div>
            </li>
        </ul>
        {% endif %}
    </div>
</header>